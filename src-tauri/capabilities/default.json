{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "splash"],
  "permissions": [
    "core:default",
    "core:window:default",
    "opener:default",
    "updater:default",
    "process:default",
    "dialog:default"
  ]
}
