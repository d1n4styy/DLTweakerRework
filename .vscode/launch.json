{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Tauri: Dev (терминал)",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev:tauri",
      "cwd": "${workspaceFolder}"
    }
  ]
}
